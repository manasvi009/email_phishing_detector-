
  # Email Phishing Detector

  This is a code bundle for Email Phishing Detector. The original project is available at https://www.figma.com/design/kF08WMeBCYCsnigYy8xDX5/Email-Phishing-Detector.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  